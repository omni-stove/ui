import { forwardRef } from 'react';
import { Text } from 'react-native';

// biome-ignore lint:
type Props = {
  // Add any additional props here
};

export const {{pascalCase name}} = forwardRef<Text, Props>((props, ref) => {
  return <Text {...props} ref={ref} />;
});
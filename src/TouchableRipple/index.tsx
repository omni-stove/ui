/**
 * A wrapper for making views respond properly to touches.
 * On Android, it corresponds to `TouchableNativeFeedback`.
 * On iOS, it corresponds to `TouchableOpacity`.
 * This component is a re-export of the TouchableRipple from React Native Paper.
 *
 * @see {@link https://callstack.github.io/react-native-paper/docs/components/TouchableRipple/|React Native Paper TouchableRipple}
 */
export { TouchableRipple } from "react-native-paper";

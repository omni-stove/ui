/**
 * A component to display a dialog.
 * Dialogs inform users about a task and can contain critical information, require decisions, or involve multiple tasks.
 * This component is a re-export of the Dialog from React Native Paper.
 * It includes sub-components like `Dialog.Title`, `Dialog.Content`, `Dialog.Actions`, and `Dialog.ScrollArea`.
 *
 * @see {@link https://callstack.github.io/react-native-paper/docs/components/Dialog/|React Native Paper Dialog}
 */
export { Dialog } from "react-native-paper";

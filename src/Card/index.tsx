/**
 * A component to display content and actions about a single subject.
 * This component is a re-export of the Card from React Native Paper.
 * It includes sub-components like `Card.Title`, `Card.Content`, `Card.Cover`, and `Card.Actions`.
 *
 * @see {@link https://callstack.github.io/react-native-paper/docs/components/Card/|React Native Paper Card}
 */
export { Card } from "react-native-paper";

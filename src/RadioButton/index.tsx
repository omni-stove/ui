/**
 * A component to display a radio button.
 * Radio buttons allow the user to select one option from a set.
 * This component is a re-export of the RadioButton from React Native Paper.
 * It includes sub-components like `RadioButton.Group`, `RadioButton.Item`, `RadioButton.Android`, and `RadioButton.IOS`.
 *
 * @see {@link https://callstack.github.io/react-native-paper/docs/components/RadioButton/|React Native Paper RadioButton}
 */
export { RadioButton } from "react-native-paper";
